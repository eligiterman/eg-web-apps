<h1>Contacts</h1>

<% contacts = Contact.all %>

<ul>
  <% for contact in contacts %>
    <% company = Company.find_by("id" => contact["company_id"]) %> 
    <li><%= "Contact Name: #{contact["first_name"]} #{contact["last_name"]} | Company: #{company["name"]}" %></li>
  <% end %>

</ul>